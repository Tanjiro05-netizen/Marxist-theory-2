# Taken up at build time by src/app/providers.jsx (NEXT_PUBLIC_ vars are
# inlined when `next build` runs, so this activates maintenance mode on
# production/Vercel deployments while local `next dev` is unaffected).
# Flip to false (or delete this file) and redeploy to bring the site back.
NEXT_PUBLIC_MAINTENANCE_MODE=true
